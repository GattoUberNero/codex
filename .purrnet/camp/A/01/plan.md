# Plan

1. Find the current delayed auto continuation data path and decide where the TUI should source the visible countdown.
2. Implement an explicit, user-visible countdown state in the TUI so delayed auto continuation is obvious and actionable.
3. Add snapshot and behavioral coverage for the visible countdown and keep the existing auto-cancel behavior intact.
